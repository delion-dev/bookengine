{
  "version": "1.0",
  "description": "Default model identifiers and gateway tuning parameters. All values are overridable via environment variables (see model_gateway.py _resolve_gateway_config). Update this file to change model defaults without touching code.",

  "default_models": {
    "text_model":       "gemini-2.5-pro",
    "structured_model": "gemini-2.5-pro",
    "research_model":   "gemini-2.5-pro",
    "safety_model":     "gemini-2.5-flash",
    "fast_model":       "gemini-2.5-flash"
  },

  "gateway_defaults": {
    "timeout_seconds":          90,
    "request_min_interval_ms":  2000,
    "request_jitter_ms":        0,
    "max_retries":              2,
    "retry_backoff_seconds":    6.0,
    "api_version":              "v1",
    "gemini_api_version":       "v1beta"
  }
}
