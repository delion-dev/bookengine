# BookEngine 빌드

Tauri 데스크톱 앱을 빌드합니다.

```bash
cd d:/solar_book/frontend && npm run tauri build
```

결과물:
- `src-tauri/target/release/bundle/msi/` — MSI 설치파일
- `src-tauri/target/release/bundle/nsis/` — NSIS 설치파일
